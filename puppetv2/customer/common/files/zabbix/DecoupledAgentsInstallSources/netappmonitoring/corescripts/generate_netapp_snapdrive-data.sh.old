#!/bin/sh

host=$1

profile=$2

#mkdir -p /tmp/netapp_data_snapdrive

outputdir=/tmp/netapp_data_snapdrive

datafile_data=$outputdir/$host-$profile"_data"
#

ssh $host "smo backup list -profile '$profile' -data" >$datafile_data

exit 0;
