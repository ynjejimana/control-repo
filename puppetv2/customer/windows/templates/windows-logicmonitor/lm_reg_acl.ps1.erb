try {
  $acl = Get-Acl '<%= @aclpath %>'
  $rule = New-Object System.Security.AccessControl.RegistryAccessRule ('<%= @upn %>','FullControl','ContainerInherit, ObjectInherit','None','Allow')
  $acl.SetAccessRule($rule)
  $acl |Set-Acl -Path '<%= @aclpath %>'
}
catch {
  Write-Error $_

  exit 1
}