# This file records what is passed to the windows::windows-qualys::agent puppet class in the 'https_proxy' parameter.
# If this file changes, the values passed in this parameter have changed, so the proxy configure script exec should run.
<%= @https_proxy %>
