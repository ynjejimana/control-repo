---
# IPv6 disabled by default
#common::ipv6::status::isEnabled: false

# IPTables disabled by default
#common::iptables::status::isEnabled: false

# Puppet configuration
#common::puppet::client::puppet_server: "master.lab.com"
#common::puppet::client::version: "5.5.22"
#common::puppet::puppetmaster::version: "5.5.22"
# New puppet client class :D
#common::puppet::client_new::version: {}

# By default, don't touch the hosts file
#common::hosts::default::hosts: {}

# By default, turn the autoupdate functionality off
#common::puppet::cron::autoupdate_cron: false
#common::puppet::cron::noop_cron: true
#common::puppet::client::update_puppet_cron: false

# LEAVE THIS VARIABLE BLANK. The classes variable is used to apply classes to a host.
# If this variable is set here every puppet client globally would have the value applied.
#classes: ""

# System users
common::users::dev_group::group_name: "labdevs"
common::users::net_group::group_name: "labnets"
common::users::uni_group::group_name: "labunix"
common::users::recovery_account::password: "98efdd090e868e55c76761aaec6cea54"
common::users::root_account::password: "98efdd090e868e55c76761aaec6cea54"
common::users::admin_group::group_name: "labadmins"

# Current UNIX team users
common::users::unix_accounts::current_users:
 uuser1:
  fullname: Unix User1
  uid: 5001
  email: unix.user1@lab.com
  rsa_ssh_key:
 uuser2:
  fullname: Unix User2
  uid: 5002
  email: unix.user2@lab.com
  rsa_ssh_key: ""

common::users::unix_accounts::former_users:

common::users::developers_accounts::former_users:

common::users::developers_accounts::current_users:
 duser1:
  fullname: Dev User1
  email: dev.user1@lab.com
  uid: 7001
  rsa_ssh_key: {}

 duser2:
  fullname: Dev User2
  email: dev.user2@lab.com
  uid: 7002
  rsa_ssh_key: {}
 
common::users::networking_accounts::current_users:
 nuser1:
  fullname: Net User1
  email: net.user1@lab.com
  uid: 7003
  rsa_ssh_key: {}

 nuser2:
  fullname: Net User2
  email: net.user2@lab.com
  uid: 7004
  rsa_ssh_key: {}

common::users::networking_accounts::former_users:

common::resolv::resolvconf::search_domains: ["lab.com"]

common::motd::default::message: |
 |------------------------------------------------------------------------------|
 | This system is managed by NTT Communications ICT Solutions (+61 2 9563 2700).|
 |                                                                              |
 | This system is for the use of authorized users only. Individuals using this  |
 | computer system without authority, or in excess of their authority, are      |
 | subject to having all of their activities on this system monitored and       |
 | recorded by system personnel.                                                |
 |                                                                              |
 | In the course of monitoring individuals improperly using this system, or in  |
 | the course of system maintenance, the activities of authorized users may     |
 | also be monitored.                                                           |
 |                                                                              |
 | Anyone using this system expressly consents to such monitoring and is        |
 | advised that if such monitoring reveals possible evidence of criminal        |
 | activity, system personnel may provide the evidence of such monitoring to    |
 | law enforcement officials.                                                   |
 |------------------------------------------------------------------------------|

# Packages to be installed on all Linux SOE VMs
#soe::linux::packages:
# - libselinux-python

# By default, root password is managed by puppet
common::users::root_account::manage_password: true

# By default, root password is not expired (-1)
common::users::root_account::password_max_age: "-1"

